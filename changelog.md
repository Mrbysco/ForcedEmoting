* Improve animation compatibility. You should now be able to animate most mobs.
* Add command to force an emote onto an entity/player: `/forcedemoting force_emote <entity/player> <emote_id> <seconds> <immobilize>`
* You can now specify the range inside the emote trigger JSON, as well as if the trigger words are case-sensitive (by default they are not case-sensitive)
* Fix player animations not moving the extra layer